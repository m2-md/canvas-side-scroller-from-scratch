{
  "name": "canvas-side-scroller-from-scratch",
  "version": "1.0.0",
  "description": "2D side-scroller engine on HTML5 Canvas: smooth lerping camera, multi-layer parallax, coyote time, jump buffering, moving platforms, and buoyancy physics.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
