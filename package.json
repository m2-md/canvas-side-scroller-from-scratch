{
  "name": "canvas-side-scroller-from-scratch",
  "version": "1.0.0",
  "description": "KAYIP ZEYTİN — Canvas'ta sıfırdan side-scroller: elle kurulmuş kamera + parallax, daire-dikdörtgen çözümleme, coyote/buffer/değişken zıplama, hareketli platform taşıma, buoyancy",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
